# ⚡ სწრაფი დაწყება / Quick Start

**5 წუთში გააშვი სოციალური მედიის მარკეტინგის აგენტი!**

---

## 🎯 რა არის ეს პროექტი?

ავტომატური AI აგენტი, რომელიც:
1. ✍️ **იწერს** Facebook პოსტებს ქართულ ენაზე (Google Gemini)
2. 🎨 **ქმნის** პროდუქტის ფოტოებს (Google Vertex AI Imagen)
3. 📱 **აგზავნის** Telegram-ში დასამტკიცებლად
4. ✏️ **საშუალებას იძლევა** რედაქტირების (Streamlit Dashboard)

---

## 🚀 სწრაფი გაშვება (ლოკალურად)

### 1. Prerequisites

```bash
# Python 3.10+ აუცილებელია
python --version
```

### 2. Clone & Install

```bash
# Clone repository
git clone <your-repo-url>
cd CC

# შექმენი virtual environment
python -m venv venv

# Activate (Mac/Linux)
source venv/bin/activate

# Activate (Windows)
venv\Scripts\activate

# Install dependencies
pip install -r requirements.txt
```

### 3. Google Cloud Setup (5 წუთი)

#### a) Gemini API Key
1. გადადი: https://ai.google.dev/
2. **Get API Key** → დააკოპირე

#### b) Vertex AI Setup
1. https://console.cloud.google.com/
2. შექმენი პროექტი
3. გააქტიურე Vertex AI API
4. შექმენი Service Account (IAM & Admin)
5. ჩამოტვირთე JSON key

### 4. Telegram Bot

1. Telegram-ში: @BotFather
2. `/newbot` → მიყევი ინსტრუქციებს
3. დააკოპირე token

### 5. Configure

შექმენი `.env` ფაილი:

```bash
TELEGRAM_BOT_TOKEN=your_bot_token_here
GOOGLE_GEMINI_API_KEY=your_gemini_key_here
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
GCP_LOCATION=us-central1
```

### 6. Run!

**Terminal 1 - Bot:**
```bash
python main.py
```

**Terminal 2 - Dashboard:**
```bash
streamlit run streamlit_dashboard.py
```

### 7. Test

Telegram-ში გაუგზავნე შენს bot-ს:
```
/create ბროწეულის ძმარი
```

---

## 🚂 Railway Deploy (10 წუთი)

სრული ინსტრუქცია: [DEPLOY_RAILWAY.md](DEPLOY_RAILWAY.md)

**სწრაფად:**

1. **Google Setup** (იგივე, რაც ზემოთ)
2. **Service Account JSON → Base64:**
   ```bash
   cat service-account-key.json | base64 > creds-base64.txt
   ```
3. **GitHub-ზე Push:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git push
   ```
4. **Railway:** https://railway.app/
   - Deploy from GitHub
   - Add Variables:
     - `TELEGRAM_BOT_TOKEN`
     - `GOOGLE_GEMINI_API_KEY`
     - `GOOGLE_CLOUD_PROJECT`
     - `GOOGLE_CREDENTIALS_BASE64` (paste from creds-base64.txt)

5. **Done!** Bot მუშაობს Railway-ზე 24/7

---

## 📱 როგორ გამოვიყენო?

### ძირითადი ბრძანებები:

```
/start          - დაწყება
/create [ძმარი] - ახალი პოსტის შექმნა
/status         - დრაფტების სტატუსი
/help           - დახმარება
```

### მაგალითი:

```
/create ბროწეულის ძმარი
```

Bot დააბრუნებს:
- 🎨 ფოტო (Vertex AI Imagen)
- 📝 ქართული ტექსტი (Gemini)
- 🎛️ ღილაკები:
  - ✅ დადასტურება
  - 🔄 ტექსტის შეცვლა
  - 🎨 ფოტოს შეცვლა
  - ✏️ Dashboard-ზე რედაქტირება
  - ❌ უარყოფა

---

## 📁 პროექტის სტრუქტურა

```
CC/
├── main.py                    # 🚀 Main app
├── config.py                  # ⚙️ Configuration helper
├── text_generator.py          # 📝 Gemini text
├── image_generator.py         # 🎨 Imagen images
├── telegram_bot.py            # 🤖 Bot logic
├── streamlit_dashboard.py     # 🌐 Web UI
├── database.py                # 💾 SQLite DB
├── requirements.txt           # 📦 Dependencies
├── Procfile                   # 🚂 Railway config
├── .env.example               # 🔐 Env template
└── README.md                  # 📖 Full docs
```

---

## 🎓 მაგალითები

### მაგალითი 1: ბროწეულის ძმარი
```
/create ბროწეულის ძმარი
```

Gemini დააგენერირებს:
```
🍯 ბუნებრივი ბროწეულის ძმარი - თქვენი ჯანმრთელობის საუკეთესო მეგობარი!

ეს ძვირფასი პროდუქტი არის სავსე ანტიოქსიდანტებითა და ვიტამინებით,
რომლებიც აძლიერებენ თქვენს იმუნიტეტს და აძლევენ ენერგიას.
მოყვანილია გარემოსდაცვით ქართულ მთებში.

შეუკვეთეთ დღესვე! 💚
```

Imagen შექმნის:
- რეალისტური ფოტო
- ძმრის ბოთლი ხის მაგიდაზე
- ბუნებრივი განათება
- პროფესიონალური ხარისხი

### მაგალითი 2: რედაქტირება Dashboard-ში

1. Bot-ში დააჭირე **✏️ დაშბორდზე რედაქტირება**
2. გახსენი Dashboard: http://localhost:8501
3. შეცვალე ტექსტი ხელით
4. **💾 შენახვა**
5. Telegram-ში ხელახლა გამოჩნდება განახლებული ვერსია

---

## ❓ ხშირი კითხვები

### Q: რამდენი ჯდება?
**A:** Google Gemini API: უფასო (60 requests/min)
     Vertex AI Imagen: ~$0.02-0.04 per image
     Railway: $5 free credit/month

### Q: მუშაობს ქართულ ენაზე?
**A:** დიახ! Gemini-ს შეუძლია ქართული ენა.

### Q: როგორ შევცვალო სურათის სტილი?
**A:** `image_generator.py` → შეცვალე `prompt` პარამეტრი

### Q: მონაცემები სად ინახება?
**A:** SQLite database (`drafts.db`), ლოკალურად ან Railway-ზე

---

## 🔧 კონფიგურაცია

### Gemini მოდელის შეცვლა

`.env`:
```bash
GEMINI_MODEL=gemini-1.5-pro  # უფრო ძლიერი, მაგრამ ნელი
# ან
GEMINI_MODEL=gemini-1.5-flash  # სწრაფი და უფასო (default)
```

### სურათის aspect ratio

`image_generator.py`:
```python
aspect_ratio="1:1"   # Instagram square
# ან
aspect_ratio="16:9"  # Facebook landscape
```

---

## 🆘 დახმარება

**პრობლემა:** Bot არ პასუხობს

**გადაწყვეტა:**
```bash
# შეამოწმე bot token
curl https://api.telegram.org/bot<TOKEN>/getMe

# ნახე logs
python main.py  # Console-ში ნახავ შეცდომებს
```

**პრობლემა:** Imagen არ მუშაობს

**გადაწყვეტა:**
```bash
# გააქტიურე API
gcloud services enable aiplatform.googleapis.com

# შეამოწმე credentials path
echo $GOOGLE_APPLICATION_CREDENTIALS
```

---

## 📚 დამატებითი რესურსები

- [README.md](README.md) - სრული დოკუმენტაცია
- [DEPLOY_RAILWAY.md](DEPLOY_RAILWAY.md) - Railway deploy გზამკვლევი
- [Google Gemini Docs](https://ai.google.dev/docs)
- [Vertex AI Imagen Docs](https://cloud.google.com/vertex-ai/docs/generative-ai/image/overview)

---

## 🎉 მზად ხარ!

ახლა შენს ხელთ გაქვს სრული AI-powered მარკეტინგის სისტემა!

**შემდეგი ნაბიჯები:**
1. ✅ გაუშვი ლოკალურად
2. ✅ ატესტე Telegram-ში
3. ✅ Deploy Railway-ზე
4. 🚀 დაიწყე მარკეტინგი!

**მადლობა, რომ იყენებ ჩვენს სისტემას!** 🍯
